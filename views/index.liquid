{% render 'partials/head.liquid' %}

<article>
  <header>
    <h1>Hallo</h1>
    <p>mijn naam is</p>
  </header>

  <p>{{ Sidney 'Sid' Hermus }}</p>

  <footer></footer>
</article>

{% render 'partials/foot.liquid' %}
